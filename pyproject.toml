[project]
name = "ghazi-options-radar"
version = "1.0.0"
description = "Source-aware US options scanner and alerting system"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
