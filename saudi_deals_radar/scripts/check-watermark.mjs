#!/usr/bin/env node
import fs from "node:fs";const html=fs.readFileSync("public/index.html","utf8"),css=fs.readFileSync("public/styles.css","utf8"),js=fs.readFileSync("public/app.js","utf8"),fails=[];if(!html.includes('data-signature="رأس الزعراء"'))fails.push("signature marker");if(!js.includes("رأس الزعراء"))fails.push("visible watermark text");if(!css.includes("Aref Ruqaa"))fails.push("Ruqaa font");if(!/position:fixed/.test(css)||!/inset:0/.test(css))fails.push("full viewport");if(!/pointer-events:none/.test(css))fails.push("interaction safety");if(fails.length){console.error("WATERMARK_FAIL",fails);process.exit(1)}console.log("WATERMARK_OK رأس الزعراء");
